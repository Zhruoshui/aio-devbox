# Minimal preset: the always_on baseline only (node + python are baked
# unconditionally by gen; their version selection is pinned here). CI copies
# this to .aio/enabled.toml to build the minimal variant. `scenarios = []` is a
# legal selection -> Dockerfile.base is just head + tail + always_on fragments.
scenarios = []

[[versions]]
id = "node"
label = "22.23.2"

[[versions]]
id = "python"
label = "3.13.0"
