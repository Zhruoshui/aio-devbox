# Scenario metadata consumed by `aio-config tui` (listing) and `gen`
# (fragment resolution). The fragment.Dockerfile next to this file holds the
# actual install steps.

id = "uv"
name = "uv"
description = "uv(Python 包/项目管理器,astral-sh aqua 后端),装 /opt/mise/installs;含 uv/uvx。体积小(~30MB),常与 ruff 搭配"
category = "lang"

[[versions]]
label = "0.5.11"
version = "0.5.11"
