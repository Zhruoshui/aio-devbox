// aio-mgr: multi-sandbox management control plane (sandbox-mgr task).
//
// Phase 0 placeholder: the workspace needs this crate to exist, but the
// mgr-api server (lifecycle, compose generation, SQLite state) is Phase 1.
// This main intentionally does nothing yet.

fn main() {
    // Filled in by sandbox-mgr Phase 1 (mgr-api server bootstrap).
}
