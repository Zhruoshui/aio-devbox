// panes/models — unified model config page, split into focused sub-modules.
// Re-export keeps App.tsx's `import { ModelsPane } from "./panes/models"`
// stable while the implementation lives in ModelsPane + the view components.

export { ModelsPane } from "./ModelsPane";
